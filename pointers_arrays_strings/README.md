# Pointers, Arrays, and Strings

This directory contains C programs focused on understanding:
- Pointers (dereferencing, pointer arithmetic).
- Arrays and their memory layout.
- String manipulation and null termination.

## Tasks:
- 0-reset_to_98.c: A function that takes a pointer to an int and updates the value to 98.
