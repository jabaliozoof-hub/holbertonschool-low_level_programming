# C - Variadic functions

This project is part of the low-level programming curriculum at Holberton School. It focuses on learning how to use variadic functions (`stdarg.h`, `va_start`, `va_arg`, `va_end`) and the `const` type qualifier in C.

## Tasks
- **0. Beauty is variable, ugliness is constant**: A function that returns the sum of all its parameters.
